// Commitlint
export type CommitEmoji = {
	type: string;
	code: string;
	emoji: string;
	description: string;
};

// Eslint
export type EslintConfig = {
	// rules: Record<string, string>;
};

// Prettier
export type PrettierConfig = {
	// rules: Record<string, string>;
};
